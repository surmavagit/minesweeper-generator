# Minesweeper Map Generator

To launch the map generator locally, run `node index.js`

The server is hard-coded to listen on port 8000.

The docker image is available at DockerHub as surmava/minesweeper-generator
